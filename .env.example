# Which LLM provider to use: "openai", "anthropic", or "gemini"
LLM_PROVIDER=openai

# API keys — only the one for your chosen provider is required
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Optional overrides (sensible defaults are set per provider)
# LLM_API_BASE=https://api.openai.com/v1
# MODEL_FAST=gpt-4o-mini
# MODEL_GENERAL=gpt-4o
# MODEL_STRONG=gpt-4.1
